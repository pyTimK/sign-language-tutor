interface QuasarPageProps {}

const QuasarPage: React.FC<QuasarPageProps> = ({}) => {
  return (
    <div
      className="flex items-center justify-center"
      style={{ width: "100vw", height: "100vh" }}
    >
      <h1>💥</h1>
    </div>
  );
};

export default QuasarPage;
